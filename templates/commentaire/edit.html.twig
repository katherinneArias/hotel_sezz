{% extends 'base.html.twig' %}

{% block title %}Modifier le commentaire{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="mb-4">Modifier le commentaire</h1>

        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-primary">Mettre à jour</button>
        {{ form_end(form) }}

        <a href="{{ path('app_commentaire_admin_index') }}" class="btn btn-outline-secondary mt-3">← Retour à la liste</a>

        {{ include('commentaire/_delete_form.html.twig', { commentaire: commentaire }) }}
    </div>
{% endblock %}