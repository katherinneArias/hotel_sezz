{% extends 'base.html.twig' %}

{% block title %}:Modifier Chambre{% endblock %}

{% block body %}
    <h1>Modifier Chambre</h1>

    {{ include('chambre/_form.html.twig', {'button_label': 'Update'}) }}

  <a href="{{ path('app_chambre_show', {'id': chambre.id}) }}" class="btn btn-warning mt-3">← Retour à la chambre</a>
  
{{ include('chambre/_delete_form.html.twig') }}
{% endblock %}
